import './globals.css';
export const metadata={title:'Driver SuperApp Admin',description:'Operations, finance and platform administration'};
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
