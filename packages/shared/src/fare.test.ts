import{describe,expect,it}from'vitest';import{calculateFare,calculateSharedPassengerFare}from'./fare';const config={baseMinor:5000,perKmMinor:2500,perMinuteMinor:500,minimumFareMinor:12000};describe('fare engine',()=>{it('calculates integer distance/time fare',()=>expect(calculateFare(config,{distanceKm:2,durationMinutes:4}).totalMinor).toBe(12000));it('enforces minimum fare',()=>expect(calculateFare(config,{distanceKm:0,durationMinutes:0}).totalMinor).toBe(12000));it('caps percentage promo',()=>{const r=calculateFare(config,{distanceKm:10,durationMinutes:10,promo:{kind:'percentage',value:100,maxDiscountMinor:15000}});expect(r.promoDiscountMinor).toBe(15000);expect(r.totalMinor).toBeGreaterThanOrEqual(0)});it('caps fixed promo at subtotal',()=>expect(calculateFare(config,{distanceKm:0,durationMinutes:0,promo:{kind:'fixed',value:999999}}).totalMinor).toBe(0));it('calculates independent passenger fares',()=>{const a=calculateSharedPassengerFare(config,2000,10,10),b=calculateSharedPassengerFare(config,8000,30,10);expect(a.totalMinor).not.toBe(b.totalMinor);expect(a.totalMinor).toBe(15075);expect(b.totalMinor).toBe(29700)});it('never lets shared discount create a negative fare',()=>expect(calculateSharedPassengerFare(config,0,0,100).totalMinor).toBe(0))});
