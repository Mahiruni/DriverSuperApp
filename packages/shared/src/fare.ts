export type Money=number;export type FareConfig={baseMinor:Money;perKmMinor:Money;perMinuteMinor:Money;minimumFareMinor:Money};export type FareInput={distanceKm:number;durationMinutes:number;promo?:Promo;driverAddonMinor?:Money};export type Promo={kind:'percentage'|'fixed';value:number;maxDiscountMinor?:Money};export type FareBreakdown={baseMinor:Money;distanceMinor:Money;timeMinor:Money;subtotalMinor:Money;minimumAdjustmentMinor:Money;promoDiscountMinor:Money;driverAddonMinor:Money;totalMinor:Money};const n=(v:number)=>Math.max(0,Math.round(v));export function calculateFare(c:FareConfig,i:FareInput):FareBreakdown{const baseMinor=n(c.baseMinor),distanceMinor=n(i.distanceKm*c.perKmMinor),timeMinor=n(i.durationMinutes*c.perMinuteMinor),raw=baseMinor+distanceMinor+timeMinor,minimumAdjustmentMinor=Math.max(0,c.minimumFareMinor-raw),subtotalMinor=raw+minimumAdjustmentMinor;let promoDiscountMinor=0;if(i.promo){promoDiscountMinor=i.promo.kind==='percentage'?Math.floor(subtotalMinor*Math.min(100,Math.max(0,i.promo.value))/100):n(i.promo.value);if(i.promo.maxDiscountMinor!==undefined)promoDiscountMinor=Math.min(promoDiscountMinor,n(i.promo.maxDiscountMinor));promoDiscountMinor=Math.min(promoDiscountMinor,subtotalMinor)}const driverAddonMinor=n(i.driverAddonMinor??0);return{baseMinor,distanceMinor,timeMinor,subtotalMinor,minimumAdjustmentMinor,promoDiscountMinor,driverAddonMinor,totalMinor:subtotalMinor-promoDiscountMinor+driverAddonMinor}}export function calculateSharedPassengerFare(c:FareConfig,distanceM:number,durationS:number,discountPercent=0){const baseMinor=n(c.baseMinor),distanceMinor=n(Math.max(0,distanceM)/1000*c.perKmMinor),timeMinor=n(Math.max(0,durationS)/60*c.perMinuteMinor),raw=baseMinor+distanceMinor+timeMinor,minimumAdjustmentMinor=Math.max(0,c.minimumFareMinor-raw),subtotalMinor=raw+minimumAdjustmentMinor,sharedDiscountMinor=Math.floor(subtotalMinor*Math.min(100,Math.max(0,discountPercent))/100);return{baseMinor,distanceMinor,timeMinor,minimumAdjustmentMinor,subtotalMinor,sharedDiscountMinor,totalMinor:subtotalMinor-sharedDiscountMinor}}
